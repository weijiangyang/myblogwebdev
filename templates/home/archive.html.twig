{% extends 'base.html.twig' %}

{% block title %}
{{ parent()}}{{ month }}
{% endblock %}

{% block body %}
<div class="container">
    <div class="row g-5">
        <div class="col">
            {# <h3 class="border-bottom pb-4 mv-4 fst-italic" style="color:{{ category.color }}">{{ category.name }}</h3> #}
            <div id="article-list">
                {% include "article/list.html.twig" with{articles: articles, leftcol:4, rightcol:8 } %}
            </div>
        </div>
    </div>
</div>
<div class="container d-flex justify-content-center">
    <div class="navigation">
        {{ knp_pagination_render(articles) }}
    </div>
</div>

{% endblock %}
