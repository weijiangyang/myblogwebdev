{% extends 'base.html.twig' %}

{% block title %}Hello UserController!{% endblock %}

{% block body %}
    
    
    <div class="container w-75">
    <div class="row">
        <div class="col">
            <h2 class="text-center">Profile de <span class="fst-italic">{{ app.user.fullName }}</span></h2>
            <div>
                <label for="username" class="form-label mt-4">Nom de l'utilisateur</label>
            </div>
            <div class="input-group mb-4">
                <span class="input-group-text">@</span>
                <input type="text" class="form-control" value="{{ app.user.fullName }}" readonly>
            </div>
            <div class="mb-3">
                <p>Nombre de commentaires postés: {{ app.user.comments | length }}</p>
            </div>
        </div>
    </div>



     
</div>
   
{% endblock %}
